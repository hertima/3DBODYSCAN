# Exercise GIFs

Drop animated GIFs here named by exercise id (e.g. `supino-reto.gif`, `agachamento.gif`).
The `<ExerciseMedia/>` component will auto-detect them. If a file is missing, a
ZYROX-styled silhouette placeholder renders instead.

The exercise ids match the `id` field in `src/data/library.ts`.
